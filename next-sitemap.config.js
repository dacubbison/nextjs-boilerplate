module.exports = {
  siteUrl: 'https://toptechmobile.com', // Your live site URL
  generateRobotsTxt: true, // Automatically generate robots.txt
  // Optional: Exclude pages if needed, e.g., exclude: ['/secret-page']
  // Optional: For larger sites, add sitemapSize: 5000
};